# 短剧高光点检测系统 — 设计与迭代文档

> 项目：real-time-interaction-stimulation-for-shot-form-drama  
> 模块：server/analysis  
> 日期：2026-06-05

---

## 一、问题定义

**目标：** 从短剧中自动检测"高光时刻"——剧情的精彩片段，用于前端进度条标记、精彩集锦生成。

**核心挑战：** 什么是"高光"？不是音量最大的片段，也不是画面变化最快的片段。高光是一个**多维度综合判断**——用词激烈、剧情反转、人物表情、音画配合——任何一个维度单独看都不够。

**设计原则：** 高光不是 LLM 猜的，是从多路信号交叉点算出来的。LLM 只在必要时提供语义理解（叙事转折、表情强度），最终由公式融合。

---

## 二、架构演进

### v0：纯 LLM 猜测（废弃）

最初在 `structured_extractor.py` 中让 LLM 直接输出 `key_scenes` + `highlights_auto`，完全依赖 LLM 对 30 帧文本描述的理解。

**问题：**
- LLM 没有时间精度概念，给出的高光点只是帧索引，无法对齐到视频秒级
- 没有利用音频信号（ASR 文本中的情绪线索）
- 纯粹"我认为这里是高光"，缺乏可解释性

### v1：双通道信号融合（当前架构基础）

引入 `EpisodeTimeline` 类，将音频层和视觉层分别处理后合并为统一时间轴：

```
音频: VAD → ASR → 情绪打分 → audio_emotion (0-1)
视觉: 抽帧 → VL描述 → 相邻帧变化 → visual_change (0-1)
       ↓
时间轴: 1秒网格对齐 → 多维度加权求和 → 高光得分
```

**问题：**
- 视觉变化用字重叠率计算，太粗糙。"两人对话"和"两人拔剑"在字面上重叠率高（大量相同词汇），但画面表现力迥异
- 所有维度纯信号驱动，没有叙事理解（"主角发现真相"这种安静的剧情转折完全漏掉）
- 音频情绪只靠关键词匹配，无法理解语境

### v2：LLM 介入叙事 + 画面强度（当前）

引入 LLM 在两个维度上提供语义判断：

1. **剧情转折：** `structured_extractor` 的 prompt 中加入六条判断标准，让 LLM 基于帧描述 + 前情提要识别剧情转折
2. **画面表现强度：** 用 LLM 替代字重叠率，对每帧描述打分 0-5（空镜→静态→活动→情绪→冲突→极致）

同时引入**观众视角上下文**（`ViewerContext`），跨集积累人物关系 + 关键事件，让 LLM 在分析第 N 集时知道前 N-1 集发生了什么，从而判断"这一刻是不是转折"。

### v3：表情感染力 + 说话人精准帧（最新）

**核心洞察：** 短剧的"高光"往往是人物的情绪爆发——愤怒、哭泣、震惊。这些情绪最直接的信号来自面部表情。而说话人的时间戳（ASR → speaker diarization）提供了精确的"谁在什么时候说了什么"——在这些时间点精准截帧分析表情，比盲抽 30 帧的命中率高得多。

**实现：**
- SpeakerIdentifier 识别到 spk 在 T 秒说话 → 抽 T-1s、T、T+1s 三帧
- `analyze_expression()` VL 逐帧判断表情强度 0-5
- 合并到时间轴的表情维度

---

## 三、最终架构

```
┌─────────────────────────────────────────────────────────┐
│                    EpisodeTimeline                      │
│                                                         │
│  输入:                                                   │
│    audio_segments  ← ASR 文本 + VAD 时间戳              │
│    frame_analyses  ← VL 帧描述 (30帧场景检测抽帧)       │
│    key_scenes      ← LLM 剧情转折标注                   │
│    visual_intensity ← LLM 画面强度 + speaker 表情帧     │
│                                                         │
│  处理:                                                   │
│    _score_audio_emotion()    → 用词激烈度 0-1           │
│    _merge_timeline()         → 1秒网格对齐              │
│    _compute_highlight_score() → 四维加权                │
│    _extract_highlights()     → top-K 合并输出           │
│                                                         │
│  输出:                                                   │
│    highlights: [{start, end, peak_score, reasons}]      │
│    timeline:  [{t, audio_text, expression, score}]      │
└─────────────────────────────────────────────────────────┘
```

---

## 四、四维高光公式

```
score = 0.30 × 用词激烈度
      + 0.25 × 表情感染力
      + 0.25 × 剧情反转
      + 0.20 × 音画联动
```

### 维度一：用词激烈度（权重 0.30）

**来源：** ASR 文本 → 关键词匹配 + 标点增强

**不调 LLM**，纯规则：

| 关键词 | 权重 | 说明 |
|--------|------|------|
| 死、爆发 | 0.9 | 致命冲突 |
| 跪下、杀、救命 | 0.85 | 极端场景 |
| 真相、住手 | 0.8 | 关键揭露 |
| 不可能、恨 | 0.75 | 强烈情绪 |
| 为什么、站住、滚、哭 | 0.7 | 冲突对抗 |
| 求 | 0.65 | 恳求/绝望 |
| 爱 | 0.6 | 情感流露 |
| 终于、原来 | 0.55 | 揭示 |
| 哈哈哈、不 | 0.5 | 情绪表达 |
| 走 | 0.4 | 低强度 |

标点增强：`！+0.15`，`？+0.1`，短句(≤5字) `+0.1`，封顶 1.0。

**设计考量：** 为什么不调 LLM？关键词匹配速度快（零 API 调用）、可解释性强、短剧对白高度模式化（"你杀了我父亲！""我要你偿命！"），关键词覆盖率高。LLM 情绪分析留给更复杂的剧情反转维度。

### 维度二：表情感染力（权重 0.25）

**来源：** 两条路径合并

**路径 A：场景帧 LLM 评分**
- 30 帧场景检测抽帧 → VL 描述 → `score_visual_intensity()` LLM 逐帧打分 0-5
- 归一化到 0-1

**路径 B：说话人精准帧（新增）**
- SpeakerIdentifier 定位说话人时间 T
- T-1s、T、T+1s 各抽一帧 → `analyze_expression()` VL 判断表情强度
- 注入时间轴对应秒

**评分标准（0-5）：**

| 分 | 含义 | 参考描述 |
|----|------|----------|
| 0 | 面无表情 | "男子面无表情直视前方" |
| 1 | 微表情 | "女子轻皱眉头，若有所思" |
| 2 | 明显情绪 | "男子露出微笑，眼神温和" |
| 3 | 强烈情绪 | "女子怒目圆睁，咬牙切齿" |
| 4 | 极端情绪 | "男子面目狰狞，青筋暴起" |
| 5 | 失控 | "女子瘫坐地上掩面嚎啕，面部扭曲" |

**设计考量：** 两条路径互补。场景帧覆盖整集（但只有 30 帧），说话人帧精确（但只在有人说话时才有）。两者分数在时间轴上取 max——场景帧可能漏掉的表情瞬间，说话人帧补上。

### 维度三：剧情反转（权重 0.25）

**来源：** LLM 结构化提取 → `key_scenes`

**注入方式：** `structured_extractor` 的 prompt 接受六条剧情转折判断标准：
1. 新人物登场：重要角色首次出场
2. 信息揭露：秘密/真相/身份被揭开
3. 关系变化：结盟/背叛/决裂/和解
4. 冲突爆发：争吵/打斗/对峙达到顶点
5. 命运转折：获得/失去重要物品或权力
6. 悬念设置：每集结尾的 cliffhanger

**观众视角上下文：** `ViewerContext` 跨集积累前情提要，注入 LLM prompt：

```
【前情提要 — 第1-2集已知信息】
已登场人物:
  张三(protagonist, 第1集登场) [李四:敌对]
关键事件时间线:
  第1集: 张三被逐出家门
  第2集: 李四夺走家产
待解决的剧情线:
  - 张三发誓要回来复仇
```

这样 LLM 在分析第 3 集时知道 "张三和李四是敌人"，当看到"张三救下李四"时能判断这是关系反转（critical）。

**重要性映射：**
- critical → 1.0
- high → 0.8
- medium → 0.5
- low → 0.2

注入到对应 timestamp ±3 秒范围的所有时间段。

### 维度四：音画联动（权重 0.20）

**来源：** 纯数学

```
av_synergy = audio_intensity × expression_intensity
```

当用词激烈 **且** 表情强烈同时发生时，乘积放大。单独一个维度高不加分——必须两个同时爆发才有联动效果。

**设计考量：** 这是"锦上添花"维度。一场哭戏如果 ASR 文本也检测到"死""杀"等关键词，同步加成会把这个片段推到更高排名。

---

## 五、时间轴构建

**网格精度：** 1 秒

**构建流程：**
1. 音频层：遍历 `audio_segments`，找到覆盖当前秒的对话段，提取 text/speaker/emotion
2. 视觉层：遍历 `frame_with_change`（30 帧 VL 描述 + speaker 帧表情），找最近的帧，提取 description/characters/expression
3. 叙事层：遍历 `key_scenes`，将 importance 值注入到对应 timestamp ±3 秒范围
4. 评分：每段独立计算四维加权分数

**高光提取：**
1. 筛选 score ≥ 0.15 的片段
2. 按时间排序 → 相邻且间距 < 10 秒的合并为一个区间
3. 按 peak_score 降序取 top-K（默认 8 个）
4. 最终按时间排序输出

---

## 六、涉及的模型

| 模型 | 用途 | 调用频率 |
|------|------|----------|
| fsmn-vad (FunASR) | 人声检测 | 1次/集 |
| paraformer-zh (FunASR) | 语音转文字 | 每 VAD 片段 1 次 |
| CAMPPlus (FunASR) | 说话人分离 | 1次/集 |
| doubao-1.5-vision-pro-32k (Ark) | 帧描述 + 表情识别 + 说话人识别 | 30帧 + speaker帧 + 表情帧 |
| doubao-1.5-pro-32k (Ark) | 结构化提取 + 剧情转折 + 画面强度评分 | 1次/集 |

---

## 七、数据流与文件

```
run_analysis.py
  ├─ scene frames → frame_cache.json (SHA256 帧缓存)
  ├─ audio analysis → (无持久化，用完清理)
  ├─ timeline → database/timeline_{剧名}_{集}.json
  ├─ analysis → database/analysis_{剧名}.json
  ├─ highlights → analysis.json → ai_analysis.highlights_auto (百分比)
  │                            → ai_analysis.highlight_intervals (区间详情)
  └─ viewer context → (内存中跨集传递，不落盘)
```

---

## 八、已知限制与改进方向

1. **用词激烈度依赖关键词表。** 短剧对白模式化，当前覆盖较好。但对古装剧特有的表达（"纳命来""休走"）缺失。改进：扩展到古装剧专用词表。

2. **剧情反转纯依赖 LLM 单次判断。** 没有二次验证。如果 LLM 漏标，该维度直接为 0。改进：加一个二次确认 LLM 调用，专用于高 candidate 片段的复核。

3. **表情识别在当前帧描述文本上进行（非原始图像）。** VL 模型先产出了帧描述，LLM 再对描述文本评分。存在信息损失。改进：直接用 VL 模型的视觉理解能力同时产出描述 + 表情分。

4. **音画联动用简单乘积。** 无法捕捉"声音平静但画面极度紧张"的反差效果。改进：加入反差维度（|audio - expression| 大时额外加分）。

5. **跨集上下文只注入 LLM prompt，不影响公式。** 同一集内的剧情转折权重不因历史而改变。改进：根据剧情积累程度动态调整 narrative 权重（前期集低、高潮集高）。

---

## 九、代码索引

| 功能 | 文件 | 关键方法 |
|------|------|----------|
| 高光公式 | graph_builder.py | `EpisodeTimeline._compute_highlight_score()` |
| 时间轴构建 | graph_builder.py | `EpisodeTimeline.build()` + `_merge_timeline()` |
| 用词激烈度 | graph_builder.py | `EpisodeTimeline._score_audio_emotion()` |
| 观众上下文 | graph_builder.py | `ViewerContext.to_prompt_context()` |
| 画面强度 | structured_extractor.py | `StructuredExtractor.score_visual_intensity()` |
| 表情识别 | multimodal_analyzer.py | `MultimodalAnalyzer.analyze_expression()` |
| 说话人表情帧 | run_analysis.py | `_analyze_one()` 中 speaker 块 |
| 断点续传 | storage.py | `AnalysisStorage.mark_status()` / `get_completed()` |
| 帧缓存 | multimodal_analyzer.py | `FrameCache` |
